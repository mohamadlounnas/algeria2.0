import 'package:flutter/material.dart';

class AppColors {
  // Primary colors (iOS-style blue)
  static const Color primary = Color(0xFF007AFF);
  static const Color primaryDark = Color(0xFF0051C7);
  static const Color primaryLight = Color(0xFF66A6FF);

  // Neutral colors (soft, minimal)
  static const Color background = Color(0xFFF7F7F8);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFE5E5EA);
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF6E6E73);

  // Status colors
  static const Color success = Color(0xFF34C759);
  static const Color warning = Color(0xFFFF9F0A);
  static const Color error = Color(0xFFFF3B30);
  static const Color info = Color(0xFF5AC8FA);

  // Status colors for requests
  static const Color draft = Color(0xFF8E8E93);
  static const Color pending = Color(0xFFFF9F0A);
  static const Color accepted = Color(0xFF007AFF);
  static const Color processing = Color(0xFFAF52DE);
  static const Color processed = Color(0xFF34C759);
  static const Color completed = Color(0xFF0A84FF);

  // Image capture states
  static const Color imagePending = Color(0xFF8E8E93);
  static const Color imageUploading = Color(0xFFFF9F0A);
  static const Color imageUploaded = Color(0xFF5AC8FA);
  static const Color imageProcessed = Color(0xFF34C759);
  static const Color imageFailed = Color(0xFFFF3B30);
}

